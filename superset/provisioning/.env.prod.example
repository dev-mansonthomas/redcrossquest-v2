# Superset Provisioning Configuration — PROD

# Superset connection
SUPERSET_URL=https://superset.graph.redcrossquest.com
SUPERSET_ADMIN_USER=admin
SUPERSET_ADMIN_PASSWORD=

# Database connection to create in Superset
DB_CONNECTION_NAME=RCQ MySQL Prod
DB_SQLALCHEMY_URI=mysql+mysqldb://user:password@host:port/database

# Optional: Override embedding domain
EMBEDDING_ALLOWED_DOMAINS=https://graph.redcrossquest.com

# Backend configuration (optional, defaults to ../../backend/.env)
BACKEND_ENV_PATH=../../backend/.env.prod
